# Emotiv Cortex connection (default local Emotiv App)
CORTEX_URL=wss://localhost:6868

# Credentials (DO NOT COMMIT REAL VALUES)
# Obtain from Emotiv Developer portal. Keep private.
CORTEX_CLIENT_ID=your_client_id
CORTEX_CLIENT_SECRET=your_client_secret

# Optional settings
#CORTEX_LICENSE=
#CORTEX_DEBIT=1
#CORTEX_PROFILE=

# Development only: accept self-signed Cortex cert
# WARNING: Never use this in production
NODE_TLS_REJECT_UNAUTHORIZED=0

# Server
HOST=0.0.0.0
PORT=3000
AUTO_CONNECT=false

# Optional API/WS token (protects /api/* and /ws)
# If set, clients must send:
#  - HTTP:  Authorization: Bearer <token>
#  - WebSocket: ws://host:port/ws?token=<token>
#API_AUTH_TOKEN=change_me
